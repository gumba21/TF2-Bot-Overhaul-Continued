	add:
	{
		"origin" "216 8 -8"
		"targetname" "server_command"
		"classname" "point_servercommand"
	}
	{
		"origin" "360 248 24"
		"team" "blue"
		"targetname" "bot_roster_blue"
		"allowSpy" "1"
		"allowSoldier" "0"
		"allowSniper" "1"
		"allowScout" "0"
		"allowPyro" "0"
		"allowMedic" "1"
		"allowHeavy" "0"
		"allowEngineer" "0"
		"allowDemoman" "0"
		"allowClassChanges" "1"
		"classname" "bot_roster"
	}
	{
		"origin" "360 248 24"
		"team" "red"
		"targetname" "bot_roster_red"
		"allowSpy" "1"
		"allowSoldier" "0"
		"allowSniper" "1"
		"allowScout" "0"
		"allowPyro" "0"
		"allowMedic" "1"
		"allowHeavy" "0"
		"allowEngineer" "0"
		"allowDemoman" "0"
		"allowClassChanges" "1"
		"classname" "bot_roster"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot"
		"count" "99999999" // Amount of bots
		"maxActive" "2" // How many bots can be active at once
		"interval" "0.25" // How long until we spawn another
		"class" "demoman" // obvious (heavyweapons instead of heavy tho) (use "auto" to randomly pick one but its kinda shit cuz it will pick medics, spies, etc)
		"team" "auto" // obvious (use "auto" to randomly pick one)
		"action_point" "bot_moveto" // Where the bot should go if its told to.
		"initial_command" "goto action point" // One of these actions -> "goto action point", "despawn", "taunt", "cloak", "uncloak", "disguise", "build sentry at nearest sentry hint", "attack sentry at next action point"
		"suppressFire" "0" // Make bots not shoot.
		"disableDodge" "0" // Make bots not dodge.
		"actionOnDeath" "" // Make bots perform action on death. (kOnDeath_RemoveSelf or kOnDeath_MoveToSpectatorTeam)
		"useTeamSpawnPoint" "1" // Use the actual spawns instead of spawning at the origin number.
		"difficulty" "" // Force a specific difficulty.
		"retainBuildings" "1" // Keep buildings after death.
		"spawnOnlyWhenTriggered" "0" // Spawn only when told to.
		"classname" "bot_generator"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot"
		"count" "99999999" // Amount of bots
		"maxActive" "2" // How many bots can be active at once
		"interval" "0.25" // How long until we spawn another
		"class" "heavyweapons" // obvious (heavyweapons instead of heavy tho) (use "auto" to randomly pick one but its kinda shit cuz it will pick medics, spies, etc)
		"team" "auto" // obvious (use "auto" to randomly pick one)
		"action_point" "bot_moveto" // Where the bot should go if its told to.
		"initial_command" "goto action point" // One of these actions -> "goto action point", "despawn", "taunt", "cloak", "uncloak", "disguise", "build sentry at nearest sentry hint", "attack sentry at next action point"
		"suppressFire" "0" // Make bots not shoot.
		"disableDodge" "0" // Make bots not dodge.
		"actionOnDeath" "" // Make bots perform action on death. (kOnDeath_RemoveSelf or kOnDeath_MoveToSpectatorTeam)
		"useTeamSpawnPoint" "1" // Use the actual spawns instead of spawning at the origin number.
		"difficulty" "" // Force a specific difficulty.
		"retainBuildings" "1" // Keep buildings after death.
		"spawnOnlyWhenTriggered" "0" // Spawn only when told to.
		"classname" "bot_generator"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot"
		"count" "99999999" // Amount of bots
		"maxActive" "2" // How many bots can be active at once
		"interval" "0.25" // How long until we spawn another
		"class" "pyro" // obvious (heavyweapons instead of heavy tho) (use "auto" to randomly pick one but its kinda shit cuz it will pick medics, spies, etc)
		"team" "auto" // obvious (use "auto" to randomly pick one)
		"action_point" "bot_moveto" // Where the bot should go if its told to.
		"initial_command" "goto action point" // One of these actions -> "goto action point", "despawn", "taunt", "cloak", "uncloak", "disguise", "build sentry at nearest sentry hint", "attack sentry at next action point"
		"suppressFire" "0" // Make bots not shoot.
		"disableDodge" "0" // Make bots not dodge.
		"actionOnDeath" "" // Make bots perform action on death. (kOnDeath_RemoveSelf or kOnDeath_MoveToSpectatorTeam)
		"useTeamSpawnPoint" "1" // Use the actual spawns instead of spawning at the origin number.
		"difficulty" "" // Force a specific difficulty.
		"retainBuildings" "1" // Keep buildings after death.
		"spawnOnlyWhenTriggered" "0" // Spawn only when told to.
		"classname" "bot_generator"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot"
		"count" "99999999" // Amount of bots
		"maxActive" "2" // How many bots can be active at once
		"interval" "0.25" // How long until we spawn another
		"class" "scout" // obvious (heavyweapons instead of heavy tho) (use "auto" to randomly pick one but its kinda shit cuz it will pick medics, spies, etc)
		"team" "auto" // obvious (use "auto" to randomly pick one)
		"action_point" "bot_moveto" // Where the bot should go if its told to.
		"initial_command" "goto action point" // One of these actions -> "goto action point", "despawn", "taunt", "cloak", "uncloak", "disguise", "build sentry at nearest sentry hint", "attack sentry at next action point"
		"suppressFire" "0" // Make bots not shoot.
		"disableDodge" "0" // Make bots not dodge.
		"actionOnDeath" "" // Make bots perform action on death. (kOnDeath_RemoveSelf or kOnDeath_MoveToSpectatorTeam)
		"useTeamSpawnPoint" "1" // Use the actual spawns instead of spawning at the origin number.
		"difficulty" "" // Force a specific difficulty.
		"retainBuildings" "1" // Keep buildings after death.
		"spawnOnlyWhenTriggered" "0" // Spawn only when told to.
		"classname" "bot_generator"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot"
		"count" "99999999" // Amount of bots
		"maxActive" "4" // How many bots can be active at once
		"interval" "0.25" // How long until we spawn another
		"class" "soldier" // obvious (heavyweapons instead of heavy tho) (use "auto" to randomly pick one but its kinda shit cuz it will pick medics, spies, etc)
		"team" "auto" // obvious (use "auto" to randomly pick one)
		"action_point" "bot_moveto" // Where the bot should go if its told to.
		"initial_command" "goto action point" // One of these actions -> "goto action point", "despawn", "taunt", "cloak", "uncloak", "disguise", "build sentry at nearest sentry hint", "attack sentry at next action point"
		"suppressFire" "0" // Make bots not shoot.
		"disableDodge" "0" // Make bots not dodge.
		"actionOnDeath" "" // Make bots perform action on death. (kOnDeath_RemoveSelf or kOnDeath_MoveToSpectatorTeam)
		"useTeamSpawnPoint" "1" // Use the actual spawns instead of spawning at the origin number.
		"difficulty" "" // Force a specific difficulty.
		"retainBuildings" "1" // Keep buildings after death.
		"spawnOnlyWhenTriggered" "0" // Spawn only when told to.
		"classname" "bot_generator"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot"
		"count" "99999999" // Amount of bots
		"maxActive" "2" // How many bots can be active at once
		"interval" "0.25" // How long until we spawn another
		"class" "engineer" // obvious (heavyweapons instead of heavy tho) (use "auto" to randomly pick one but its kinda shit cuz it will pick medics, spies, etc)
		"team" "auto" // obvious (use "auto" to randomly pick one)
		"action_point" "bot_moveto" // Where the bot should go if its told to.
		"initial_command" "goto action point" // One of these actions -> "goto action point", "despawn", "taunt", "cloak", "uncloak", "disguise", "build sentry at nearest sentry hint", "attack sentry at next action point"
		"suppressFire" "0" // Make bots not shoot.
		"disableDodge" "0" // Make bots not dodge.
		"actionOnDeath" "" // Make bots perform action on death. (kOnDeath_RemoveSelf or kOnDeath_MoveToSpectatorTeam)
		"useTeamSpawnPoint" "1" // Use the actual spawns instead of spawning at the origin number.
		"difficulty" "" // Force a specific difficulty.
		"retainBuildings" "1" // Keep buildings after death.
		"spawnOnlyWhenTriggered" "0" // Spawn only when told to.
		"classname" "bot_generator"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot_moveto"
		"parentname" "!player"
		"stay_time" "0.5"
		"next_action_point" "bot_moveto2"
		"desired_distance" "50"
		"classname" "bot_action_point"
	}
	{
		"origin" "0 0 0"
		"targetname" "bot_moveto2"
		"parentname" "!player"
		"stay_time" "0.5"
		"next_action_point" "bot_moveto"
		"desired_distance" "50"
		"classname" "bot_action_point"
	}
	modify:
	{
		match:
		{
			"classname" "logic_auto"
		}
		insert:
		{
			"OnMapSpawn" "server_command,Command,wait 400; exec SeekAndDestroy,1,1"
			"OnMapSpawn" "bot*,Enable,,0,-1"
		}
	}